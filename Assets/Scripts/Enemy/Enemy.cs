using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour {
	// ? INTERNAL REFERENCES
	//[HideInInspector] public Transform _play; // ! references the transform of the player
	//public Transform _self; // ! references the transform of the enemy

	// ON START FRAME
	// private void Start()
	// {
	// 	//_self = transform;
	// 	_play = GameObject.FindGameObjectsWithTag("Player")[0].transform;
	// }
}
